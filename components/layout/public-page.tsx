import Link from "next/link";
import { PublicHeader } from "./public-header";
import { PublicFooter } from "./public-footer";
export function PublicPage({ eyebrow, title, intro, children, cta = "Start Free Forever", ctaHref = "/register" }: { eyebrow: string; title: string; intro: string; children: React.ReactNode; cta?: string; ctaHref?: string }) {
  return <div className="min-h-screen bg-cream text-ink"><PublicHeader /><main><section className="border-b border-sky-100 bg-gradient-to-br from-sky-50 to-white px-5 py-16 sm:px-8"><div className="mx-auto max-w-5xl"><p className="text-sm font-black uppercase tracking-[.18em] text-deepblue">{eyebrow}</p><h1 className="mt-4 max-w-4xl text-4xl font-black tracking-tight sm:text-6xl">{title}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-600">{intro}</p></div></section><div className="mx-auto max-w-5xl px-5 py-14 sm:px-8">{children}<section className="mt-16 rounded-3xl bg-skybrand p-8 text-white sm:p-10"><h2 className="text-3xl font-black">Rules match. Humans approve.</h2><p className="mt-3 max-w-2xl text-sky-50">Bring structure to your next reconciliation while keeping review decisions in human hands.</p><Link href={ctaHref} className="mt-6 inline-flex rounded-xl bg-white px-5 py-3 font-black text-deepblue">{cta}</Link></section></div></main><PublicFooter /></div>;
}
export function ContentSection({ title, children }: { title: string; children: React.ReactNode }) { return <section className="mt-12 first:mt-0"><h2 className="text-2xl font-black sm:text-3xl">{title}</h2><div className="mt-4 space-y-4 text-base leading-7 text-slate-600">{children}</div></section>; }
export function FeatureGrid({ items }: { items: { title: string; text: string }[] }) { return <div className="mt-6 grid gap-4 md:grid-cols-2">{items.map(i => <article key={i.title} className="rounded-2xl border border-slate-200 bg-white p-6"><h3 className="font-black text-ink">{i.title}</h3><p className="mt-2 leading-6 text-slate-600">{i.text}</p></article>)}</div>; }
