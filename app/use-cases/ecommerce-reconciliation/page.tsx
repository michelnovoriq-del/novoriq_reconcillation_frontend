import Link from "next/link"; import { PublicPage,ContentSection,FeatureGrid } from "@/components/layout/public-page"; import { pageMetadata } from "@/lib/metadata";
export const metadata=pageMetadata("Ecommerce Payout Reconciliation Software","Review payment-platform exports against bank records using structured mapping, normalization, deterministic matching, and exception review.","/use-cases/ecommerce-reconciliation");
export default function Page(){return <PublicPage eyebrow="Ecommerce reconciliation" title="Review ecommerce payouts against bank records" intro="Compare authorized CSV or Excel exports from payment and commerce platforms with bank transaction exports through a structured, human-reviewed workflow." cta="Try the workflow with authorized data"><ContentSection title="Why payout and bank totals may differ"><FeatureGrid items={["Processing fees","Refunds and chargebacks","Payout timing","Grouped settlements","Missing references","Different date formats","Different transaction identifiers","Platform-specific reporting"].map(x=>({title:x,text:`Treat ${x.toLowerCase()} as a review factor rather than assuming every platform or export behaves identically.`}))}/></ContentSection><ContentSection title="A practical export-based workflow"><p>Upload a payment or payout export and a bank transaction export, map their columns, normalize the records, run deterministic matching, review exceptions, and export approved results.</p><p>CSV or Excel exports from platforms such as Stripe, Shopify, PayPal, or Wise may be used when their columns fit the workflow. These are examples, not claims of official direct integrations.</p><p>See <Link href="/how-it-works" className="font-bold text-deepblue">each workflow step</Link>, guidance for <Link href="/use-cases/bookkeepers" className="font-bold text-deepblue">bookkeepers</Link>, and <Link href="/pricing" className="font-bold text-deepblue">current plan limits</Link>.</p></ContentSection></PublicPage>}
