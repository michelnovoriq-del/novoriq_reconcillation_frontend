import { PublicPage,ContentSection,FeatureGrid } from "@/components/layout/public-page"; import { pageMetadata } from "@/lib/metadata"; import { siteConfig } from "@/lib/site-config";
export const metadata=pageMetadata("Novoriq Data Retention and Deletion","Learn how Novoriq handles uploaded files, reconciliation data, generated exports, retention periods, and user-requested deletion.","/data-retention");
export default function Page(){return <PublicPage eyebrow="Data retention" title="Clear retention and deletion for reconciliation data" intro="Novoriq configures expiry periods for raw files and detailed reconciliation records. Automatic enforcement requires the production cleanup worker to be actively scheduled."><ContentSection title="Configured retention periods"><FeatureGrid items={[{title:"Raw and unprocessed uploads",text:"Files are assigned a 24-hour expiry. Deletion occurs when the configured cleanup task runs."},{title:"Generated exports",text:"Export cleanup is designed around a 24-hour window; confirm current production execution with support."},{title:"Free Forever details",text:"Detailed reconciliation data is configured for 7 days."},{title:"Professional details",text:"Detailed reconciliation data is intended for 365 days when that plan is active."},{title:"Firm details",text:"Detailed reconciliation data is intended for 730 days when that plan is active."}]}/></ContentSection><ContentSection title="Deletion choices and operational records"><p>Authorized users can delete uploaded files through the product workflow. Run-level deletion and account-wide deletion may require support depending on the current interface and data state.</p><p>Minimal account, billing, security, or operational records may be retained where needed to operate and protect the service. Deletion from backups is not represented as immediate.</p><p>To request account or data deletion, email <a className="font-bold text-deepblue" href={`mailto:${siteConfig.supportEmail}?subject=Novoriq%20Data%20Deletion%20Request`}>{siteConfig.supportEmail}</a>.</p></ContentSection></PublicPage>}
